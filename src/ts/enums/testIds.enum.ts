/**
 * Enum for test ids.
 */
export enum TestIds {
  /**
   * A valid id that exists in the database.
   */
  EXISTENT = 1,
  /**
   * An invalid id that does not exist in the database.
   */
  NON_EXISTENT = 99999,
}
